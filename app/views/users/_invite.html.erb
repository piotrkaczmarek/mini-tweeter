<%= button_to "Invite to #{@org_admined_by_current_user.name}",
               { controller: :invitations, action: :create,
                 organization_id: @org_admined_by_current_user.id,
                 user_id: @user.id },
               class: "btn btn-large btn-primary",
               method: :post %>

