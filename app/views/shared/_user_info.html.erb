<% if gravatar = gravatar_for(current_user, size: 52) %>
<%= link_to gravatar, current_user %>
<% end %>
<h1>
  <%= current_user.name %>
</h1>
<span>
  <%= link_to "view my profile", current_user %>
</span>
<span>
  <%= pluralize(current_user.microposts.count, "micropost") %>
</span>
